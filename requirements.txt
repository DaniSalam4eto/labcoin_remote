pygame>=2.5,<3
bleak>=0.22,<1
pillow>=11.3          # native AVIF support (replaces pillow-avif-plugin, which
                      # segfaults on import against Pillow 12)

yt-dlp>=2024.8
flask>=3.0
